API - /data/, /variables/, /inference/
  GET /variables/ - list of variables
  GET /data?time=xxx/ - get data in time range
  POST /data/ - add a set of data
  GET /inference/:corelation?params=...

/api/...

static server - index.html
